{# Example custom template prompt. Copy this folder, then edit. #}

{{ system_prompt }}

# Output contract

Emit a single JSON object matching this schema:

```json
{{ output_schema_json }}
```

# Transcript

{{ transcript }}

# Note (JSON):
